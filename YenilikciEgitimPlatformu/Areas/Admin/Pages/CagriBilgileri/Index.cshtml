@page
@model YenilikciEgitimPlatformu.Areas.Admin.Pages.CagriBilgileri.IndexModel
@{
    ViewData["Title"] = "Çağrı Bilgileri";
    ViewData["Subtitle"] = "Resmi kurum çağrılarını ve yarışmaları yönetin";
    Layout = "~/Pages/Shared/_LayoutDashboard.cshtml";
}

@* ════════════════════════════════════════════════════════════════════════════
   MODERN GLASSMORPHISM DASHBOARD LİSTE (INDEX SAYFASI)
   ════════════════════════════════════════════════════════════════════════════
   - Responsive Grid Layout
   - Glassmorphism Cards
   - Advanced Filtering
   - Pagination with Query String Preservation
   - AlertService Integration (No inline Swal!)
   - Accessibility Compliant
*@

<style>
    /* ═══════════════════════════════════════════════════════════════════
           CUSTOM STYLES - Glassmorphism & Card Enhancements
           ═══════════════════════════════════════════════════════════════ */

    .bg-dot-pattern {
        background-image: radial-gradient(#94a3b8 1px, transparent 1px);
        background-size: 24px 24px;
    }

    .dark .bg-dot-pattern {
        background-image: radial-gradient(#334155 1px, transparent 1px);
    }

    .glass-card {
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.5);
    }

    .dark .glass-card {
        background: rgba(30, 41, 59, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.05);
    }
</style>

@* ════════════════════════════════════════════════════════════════════════════
   MAIN CONTENT WRAPPER
   ════════════════════════════════════════════════════════════════════════════ *@

<div class="relative min-h-screen pb-12">

    @* ═══════════════════════════════════════════════════════════════════
       FULL WIDTH HEADER
       ═══════════════════════════════════════════════════════════════ *@
    <div class="relative z-10 -mx-6 lg:-mx-8 px-6 lg:px-8 py-8 mb-8 bg-gradient-to-r from-white via-gray-50/50 to-white dark:from-petrol-900 dark:via-petrol-900/50 dark:to-petrol-900 border-b border-gray-200 dark:border-petrol-800 shadow-sm">
        <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 animate__animated animate__fadeInDown">
            <div>
                <h1 class="text-3xl font-black text-petrol-900 dark:text-white tracking-tight flex items-center gap-3">
                    <span class="w-1.5 h-8 rounded-full bg-accent-500 shadow-lg shadow-accent-500/30"></span>
                    Çağrı Listesi
                </h1>
                <p class="text-sm font-medium text-gray-500 dark:text-gray-400 mt-2 ml-5">
                    @if (Model.TotalCount > 0)
                    {
                        <text>@Model.GetStartRecord() - @Model.GetEndRecord() arası gösteriliyor (Toplam <strong>@Model.TotalCount</strong> kayıt)</text>
                    }
                    else
                    {
                        <text>Kayıt bulunamadı</text>
                    }
                </p>
            </div>

            <a asp-page="./Olustur" class="group relative px-6 py-3 bg-gradient-to-r from-petrol-600 to-petrol-800 hover:from-petrol-700 hover:to-petrol-900 text-white font-bold rounded-xl shadow-lg shadow-petrol-500/20 hover:shadow-petrol-500/30 hover:-translate-y-0.5 transition-all duration-300" aria-label="Yeni Çağrı Ekle">
                <span class="flex items-center gap-2">
                    <i class="fas fa-plus group-hover:rotate-90 transition-transform"></i>
                    Yeni Çağrı Ekle
                </span>
            </a>
        </div>
    </div>

    @* ═══════════════════════════════════════════════════════════════════
       CONTENT WRAPPER
       ═══════════════════════════════════════════════════════════════ *@
    <div class="relative z-10 space-y-6 max-w-7xl mx-auto px-0">

        @* ═══════════════════════════════════════════════════════════════════
           FILTER AREA (Gelişmiş Filtreleme - Floating Labels)
           ═══════════════════════════════════════════════════════════════ *@
        <div class="glass-card rounded-2xl p-6 shadow-sm animate__animated animate__fadeInUp">
            <form method="get" class="grid grid-cols-1 md:grid-cols-12 gap-4 items-end" id="filterForm">

                @* Arama (Floating Label) *@
                <div class="md:col-span-4 relative group">
                    <input type="text" name="Filtre.Arama" value="@Model.Filtre.Arama" id="filter_search"
                           class="block px-2.5 pb-2.5 pt-5 w-full text-sm text-gray-900 bg-gray-50/50 dark:bg-petrol-900/30 rounded-xl border border-gray-200 dark:border-petrol-600 appearance-none dark:text-white focus:outline-none focus:ring-0 focus:border-accent-500 peer pl-10"
                           placeholder=" "
                           aria-label="Başlık veya Açıklama Ara" />
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <i class="fas fa-search text-gray-400 transition-colors peer-focus:text-accent-500"></i>
                    </div>
                    <label for="filter_search" class="absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-4 scale-75 top-4 z-10 origin-[0] left-10 peer-focus:text-accent-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-placeholder-shown:top-3.5 peer-focus:scale-75 peer-focus:-translate-y-4 pointer-events-none">
                        Başlık veya Açıklama Ara
                    </label>
                </div>

                @* Tür Filtresi (Floating Label) *@
                <div class="md:col-span-3 relative group">
                    <select name="Filtre.Turu" id="filter_type" asp-items="Html.GetEnumSelectList<YenilikciEgitimPlatformu.Models.CagriTuru>()"
                            class="block px-2.5 pb-2.5 pt-5 w-full text-sm text-gray-900 bg-gray-50/50 dark:bg-petrol-900/30 rounded-xl border border-gray-200 dark:border-petrol-600 appearance-none dark:text-white focus:outline-none focus:ring-0 focus:border-accent-500 peer pl-10"
                            aria-label="Çağrı Türü Filtresi">
                        <option value="">Tüm Türler</option>
                    </select>
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <i class="fas fa-tag text-gray-400 transition-colors"></i>
                    </div>
                    <div class="absolute inset-y-0 right-0 flex items-center pr-3 pointer-events-none">
                        <i class="fas fa-chevron-down text-gray-400 text-xs"></i>
                    </div>
                    <label for="filter_type" class="absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-4 scale-75 top-4 z-10 origin-[0] left-10 peer-focus:text-accent-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-placeholder-shown:top-3.5 peer-focus:scale-75 peer-focus:-translate-y-4 pointer-events-none">
                        Çağrı Türü
                    </label>
                </div>

                @* Kurum Filtresi (Floating Label) *@
                <div class="md:col-span-3 relative group">
                    <input type="text" name="Filtre.Kurum" value="@Model.Filtre.Kurum" id="filter_org"
                           class="block px-2.5 pb-2.5 pt-5 w-full text-sm text-gray-900 bg-gray-50/50 dark:bg-petrol-900/30 rounded-xl border border-gray-200 dark:border-petrol-600 appearance-none dark:text-white focus:outline-none focus:ring-0 focus:border-accent-500 peer pl-10"
                           placeholder=" "
                           aria-label="Kurum Filtresi" />
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <i class="fas fa-building text-gray-400 transition-colors peer-focus:text-accent-500"></i>
                    </div>
                    <label for="filter_org" class="absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-4 scale-75 top-4 z-10 origin-[0] left-10 peer-focus:text-accent-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-placeholder-shown:top-3.5 peer-focus:scale-75 peer-focus:-translate-y-4 pointer-events-none">
                        Kurum
                    </label>
                </div>

                @* Action Buttons *@
                <div class="md:col-span-2 flex gap-2">
                    <button type="submit" class="flex-1 h-[46px] bg-accent-500 hover:bg-accent-600 text-white rounded-xl transition-colors shadow-md shadow-accent-500/20 flex items-center justify-center gap-2" aria-label="Filtrele">
                        <i class="fas fa-filter"></i> <span class="hidden md:inline">Ara</span>
                    </button>
                    <a asp-page="./Index" class="h-[46px] w-[46px] flex items-center justify-center bg-gray-100 dark:bg-petrol-700 text-gray-600 dark:text-gray-300 rounded-xl hover:bg-gray-200 dark:hover:bg-petrol-600 transition-colors" title="Filtreleri Temizle" aria-label="Filtreleri Temizle">
                        <i class="fas fa-redo-alt"></i>
                    </a>
                </div>
            </form>
        </div>

        @* ═══════════════════════════════════════════════════════════════════
           LIST AREA (Card Grid Layout)
           ═══════════════════════════════════════════════════════════════ *@
        @if (Model.Data.Any())
        {
            <div class="grid grid-cols-1 gap-4 animate__animated animate__fadeInUp" style="animation-delay: 0.1s">
                @foreach (var cagri in Model.Data)
                {
                    <div class="group glass-card rounded-2xl p-4 hover:border-accent-500/30 hover:shadow-lg hover:shadow-accent-500/5 transition-all duration-300 relative overflow-hidden">

                        @* Hover Accent Line *@
                        <div class="absolute left-0 top-0 bottom-0 w-1.5 bg-accent-500 transform -translate-x-full group-hover:translate-x-0 transition-transform duration-300"></div>

                        <div class="flex flex-col md:flex-row gap-5">
                            @* Görsel / İkon Alanı *@
                            <div class="shrink-0 relative">
                                @if (!string.IsNullOrEmpty(cagri.KapakResmiUrl))
                                {
                                    <img src="@cagri.KapakResmiUrl" alt="@cagri.Baslik" class="w-full md:w-36 h-36 object-cover rounded-xl shadow-sm group-hover:scale-105 transition-transform duration-500" loading="lazy" />
                                }
                                else
                                {
                                    <div class="w-full md:w-36 h-36 bg-gradient-to-br from-gray-50 to-gray-100 dark:from-petrol-800 dark:to-petrol-900 rounded-xl flex flex-col items-center justify-center border border-dashed border-gray-200 dark:border-petrol-700 group-hover:border-accent-500/30 transition-colors">
                                        <i class="fas fa-image text-3xl text-gray-300 dark:text-petrol-600 mb-2"></i>
                                        <span class="text-[10px] text-gray-400 uppercase tracking-wider">Görsel Yok</span>
                                    </div>
                                }

                                @* Tür Badge (Resim Üzerinde) *@
                                <div class="absolute top-2 left-2">
                                    <span class="px-2 py-1 rounded-lg text-[10px] font-bold uppercase tracking-wider bg-white/90 dark:bg-black/80 backdrop-blur-sm text-petrol-700 dark:text-petrol-300 shadow-sm border border-white/20">
                                        @cagri.TuruText
                                    </span>
                                </div>
                            </div>

                            @* İçerik Alanı *@
                            <div class="flex-1 flex flex-col justify-between py-1">
                                <div>
                                    <div class="flex items-start justify-between gap-3">
                                        <div>
                                            <h3 class="text-xl font-bold text-gray-900 dark:text-white group-hover:text-accent-500 transition-colors line-clamp-1">
                                                @cagri.Baslik
                                            </h3>
                                            <p class="text-sm font-medium text-petrol-600 dark:text-petrol-400 mb-2 flex items-center gap-1">
                                                <i class="fas fa-building opacity-70"></i> @cagri.Kurum
                                            </p>
                                        </div>

                                        @* Durum Badge *@
                                        @if (cagri.Aktif)
                                        {
                                            <span class="shrink-0 inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-300 border border-green-200 dark:border-green-800">
                                                <span class="w-1.5 h-1.5 rounded-full bg-green-500 mr-1.5 animate-pulse"></span>
                                                Aktif
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="shrink-0 inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 border border-gray-200 dark:border-gray-700">
                                                <i class="fas fa-pause-circle mr-1.5"></i> Pasif
                                            </span>
                                        }
                                    </div>

                                    <p class="text-sm text-gray-600 dark:text-gray-400 line-clamp-2 mt-2 leading-relaxed">
                                        @cagri.KisaAciklama
                                    </p>
                                </div>

                                @* Alt Bilgi ve Butonlar *@
                                <div class="flex flex-wrap items-center justify-between mt-4 border-t border-gray-100 dark:border-petrol-700/50 pt-3 gap-4">

                                    @* Meta Bilgiler *@
                                    <div class="flex items-center gap-4 text-xs font-medium text-gray-500 dark:text-gray-400">
                                        <span class="flex items-center gap-1.5 bg-gray-50 dark:bg-petrol-800 px-2 py-1 rounded-lg">
                                            <i class="fas fa-calendar text-accent-500"></i>
                                            Bitiş: @(cagri.BitisTarihi.HasValue? cagri.BitisTarihi.Value.ToString("dd MMM yyyy", new System.Globalization.CultureInfo("tr-TR")) : "Belirtilmedi")
                                        </span>
                                        <span class="flex items-center gap-1.5" title="Görüntülenme">
                                            <i class="fas fa-eye text-petrol-400"></i> @cagri.GoruntulenmeSayisi
                                        </span>
                                        <span class="flex items-center gap-1.5" title="Takipçi">
                                            <i class="fas fa-users text-petrol-400"></i> @cagri.TakipciSayisi
                                        </span>
                                    </div>

                                    @* Action Buttons *@
                                    <div class="flex items-center gap-2">
                                        <a asp-page="/CagriBilgileri/Detay" asp-route-slug="@cagri.Slug" target="_blank" class="p-2 text-gray-400 hover:text-petrol-600 dark:hover:text-petrol-300 hover:bg-petrol-50 dark:hover:bg-petrol-800 rounded-lg transition-colors" title="Sitede Gör" aria-label="Sitede Gör">
                                            <i class="fas fa-external-link-alt"></i>
                                        </a>
                                        <a asp-page="./Duzenle" asp-route-id="@cagri.Id" class="p-2 text-gray-400 hover:text-amber-500 hover:bg-amber-50 dark:hover:bg-amber-900/20 rounded-lg transition-colors" title="Düzenle" aria-label="Düzenle">
                                            <i class="fas fa-pen"></i>
                                        </a>
                                        <button type="button" onclick="handleDelete(@cagri.Id, '@Html.Raw(cagri.Baslik.Replace("'", "\\'"))')" class="p-2 text-gray-400 hover:text-red-500 hover:bg-red-50 dark:hover:bg-red-900/20 rounded-lg transition-colors" title="Sil" aria-label="Sil">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>

            @* ═══════════════════════════════════════════════════════════════════
               PAGINATION (Filtre Query String Korumalı)
               ═══════════════════════════════════════════════════════════════ *@
            @if (Model.TotalCount > Model.Filtre.PageSize)
            {
                <div class="flex justify-center mt-8 animate__animated animate__fadeInUp">
                    <nav class="inline-flex rounded-xl shadow-sm bg-white dark:bg-petrol-900 p-1.5 border border-gray-100 dark:border-petrol-800 gap-1" aria-label="Sayfalama">
                        @if (Model.Filtre.Page > 1)
                        {
                            <a asp-page="./Index"
                               asp-route-page="@(Model.Filtre.Page - 1)"
                               asp-route-arama="@Model.Filtre.Arama"
                               asp-route-turu="@Model.Filtre.Turu"
                               asp-route-kurum="@Model.Filtre.Kurum"
                               class="px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-petrol-800 rounded-lg transition-colors"
                               aria-label="Önceki Sayfa">
                                <i class="fas fa-chevron-left mr-1"></i> Önceki
                            </a>
                        }

                        <span class="px-4 py-2 text-sm font-bold text-accent-600 dark:text-accent-400 bg-accent-50 dark:bg-accent-900/20 rounded-lg border border-accent-100 dark:border-accent-900/30" aria-current="page">
                            Sayfa @Model.Filtre.Page / @Model.GetTotalPages()
                        </span>

                        @if (Model.Filtre.Page < Model.GetTotalPages())
                        {
                            <a asp-page="./Index"
                               asp-route-page="@(Model.Filtre.Page + 1)"
                               asp-route-arama="@Model.Filtre.Arama"
                               asp-route-turu="@Model.Filtre.Turu"
                               asp-route-kurum="@Model.Filtre.Kurum"
                               class="px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-petrol-800 rounded-lg transition-colors"
                               aria-label="Sonraki Sayfa">
                                Sonraki <i class="fas fa-chevron-right ml-1"></i>
                            </a>
                        }
                    </nav>
                </div>
            }
        }
        else
        {
            @* ═══════════════════════════════════════════════════════════════════
               EMPTY STATE (Boş Durum)
               ═══════════════════════════════════════════════════════════════ *@
            <div class="flex flex-col items-center justify-center py-20 px-4 text-center glass-card rounded-3xl border border-dashed border-gray-300 dark:border-petrol-700 animate__animated animate__fadeIn">
                <div class="w-24 h-24 bg-gray-50 dark:bg-petrol-800 rounded-full flex items-center justify-center mb-6 shadow-inner ring-8 ring-gray-50/50 dark:ring-petrol-800/50">
                    <i class="fas fa-search text-4xl text-gray-300 dark:text-petrol-600"></i>
                </div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">Sonuç Bulunamadı</h3>
                <p class="text-gray-500 dark:text-gray-400 max-w-md mx-auto mb-8">
                    Aradığınız kriterlere uygun çağrı bulunamadı. Filtreleri temizlemeyi deneyin veya yeni bir çağrı oluşturun.
                </p>
                <div class="flex gap-3">
                    <a asp-page="./Index" class="px-6 py-3 bg-gray-100 dark:bg-petrol-800 text-gray-700 dark:text-gray-300 font-semibold rounded-xl hover:bg-gray-200 dark:hover:bg-petrol-700 transition-all">
                        <i class="fas fa-redo mr-2"></i>Filtreleri Temizle
                    </a>
                    <a asp-page="./Olustur" class="group relative px-6 py-3 bg-gradient-to-r from-petrol-600 to-petrol-800 hover:from-petrol-700 hover:to-petrol-900 text-white font-bold rounded-xl shadow-lg shadow-petrol-500/20 hover:shadow-petrol-500/30 hover:-translate-y-0.5 transition-all duration-300">
                        <span class="flex items-center gap-2">
                            <i class="fas fa-plus group-hover:rotate-90 transition-transform"></i>
                            Yeni Çağrı Ekle
                        </span>
                    </a>
                </div>
            </div>
        }

    </div>
</div>

@* ════════════════════════════════════════════════════════════════════════════
   HIDDEN DELETE FORM (CSRF Protected)
   ════════════════════════════════════════════════════════════════════════════ *@
<form id="deleteForm" method="post" asp-page-handler="Delete" class="hidden">
    <input type="hidden" name="id" id="deleteId" />
    @Html.AntiForgeryToken()
</form>

@section Scripts {
    <script type="module">
        // ═══════════════════════════════════════════════════════════════════
        // ALERTSERVICE ENTEGRASYONU (Merkezi Feedback Sistemi)
        // ═══════════════════════════════════════════════════════════════════

        import AlertService from '/js/modules/alertService.js';

        /**
         * Silme işlemi için merkezi fonksiyon
         * AlertService.confirmDelete kullanır (inline Swal YOK!)
         */
        window.handleDelete = async function(id, baslik) {
            // [Mimari Notu]
            // Doğrudan Swal.fire() kullanımı YASAK!
            // Tüm kullanıcı geri bildirimleri AlertService üzerinden yapılır.

            const confirmed = await AlertService.confirmDelete(
                `"${baslik}" başlıklı çağrı ve ilişkili tüm veriler kalıcı olarak silinecek.\n\nBu işlem geri alınamaz!`,
                'Silmek İstediğinize Emin misiniz?'
            );

            if (confirmed) {
                // Yükleme göstergesi
                AlertService.showLoading('Çağrı siliniyor...');

                // Form submit
                document.getElementById('deleteId').value = id;
                document.getElementById('deleteForm').submit();
            }
        }

        // ═══════════════════════════════════════════════════════════════════
        // AUTO-SUBMIT FİLTRELERİ (Debounce ile)
        // ═══════════════════════════════════════════════════════════════════

        const filterForm = document.getElementById('filterForm');
        let debounceTimer = null;

        // Text input'lar için debounce (600ms)
        const textInputs = filterForm.querySelectorAll('#filter_search, #filter_org');
        textInputs.forEach(input => {
            input.addEventListener('input', function() {
                clearTimeout(debounceTimer);
                debounceTimer = setTimeout(() => {
                    filterForm.submit();
                }, 600);
            });
        });

        // Dropdown için hemen submit
        const typeFilter = document.getElementById('filter_type');
        if (typeFilter) {
            typeFilter.addEventListener('change', function() {
                filterForm.submit();
            });
        }
    </script>
}

@* ════════════════════════════════════════════════════════════════════════════
   SAYFA AÇIKLAMASI
   ════════════════════════════════════════════════════════════════════════════
   
   [AMAÇ]
   Admin kullanıcıları için Çağrı Bilgileri yönetim sayfası.
   Modern, responsive, glassmorphism tasarımlı liste görünümü.
   
   [ÖZELLİKLER]
   - Responsive Card Grid Layout
   - Gelişmiş Filtreleme (Arama, Tür, Kurum)
   - Pagination (Query String Korumalı)
   - Lazy Loading Images
   - AlertService Entegrasyonu (Merkezi Feedback)
   - Accessibility Compliant (ARIA labels, keyboard navigation)
   - Dark Mode Support
   
   [MİMARİ]
   - ❌ ASLA inline Swal.fire() kullanma
   - ✅ HER ZAMAN AlertService.confirmDelete() kullan
   - Region kullanımı ile kod organizasyonu
   - TempData ile server-side mesajlar (_LayoutDashboard'da otomatik)
   
   [PERFORMANS]
   - Lazy loading images (loading="lazy")
   - Debounced auto-submit (600ms)
   - CSS ile animasyonlar (GPU accelerated)
   
   [GÜVENLİK]
   - CSRF token koruması
   - XSS koruması (Html.Raw escape)
   - SQL Injection koruması (EF Core)
   
   [BAĞIMLILIKLAR]
   - /js/modules/alertService.js (Merkezi feedback sistemi)
   - _LayoutDashboard.cshtml (Layout)
   - Tailwind CSS
   - Font Awesome 6.4
*@
