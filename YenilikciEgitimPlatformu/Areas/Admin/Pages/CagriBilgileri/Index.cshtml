@page
@model YenilikciEgitimPlatformu.Areas.Admin.Pages.CagriBilgileri.IndexModel
@{
    ViewData["Title"] = "Çağrı Bilgileri";
    ViewData["Subtitle"] = "Resmi kurum çağrılarını ve yarışmaları yönetin";
    Layout = "~/Pages/Shared/_LayoutDashboard.cshtml";
}

@* ════════════════════════════════════════════════════════════════════════════
   MODERN GLASSMORPHISM DASHBOARD LİSTE SAYFASI
   ════════════════════════════════════════════════════════════════════════════
   
   [ÖZELLİKLER]
   - Responsive Grid Layout
   - Glassmorphism Cards
   - Advanced Filtering
   - Pagination with Query String Preservation
   - AlertService Integration (No inline Swal!)
   - Accessibility Compliant
   - Dark Mode Support
   
   [BAĞIMLILIKLAR]
   - AlertService.js (Merkezi feedback sistemi)
   - _LayoutDashboard.cshtml (Layout)
   - Tailwind CSS
   - Font Awesome 6.4
*@

@* ════════════════════════════════════════════════════════════════════════════
   CUSTOM STYLES
   ════════════════════════════════════════════════════════════════════════════ *@
<style>
    .bg-dot-pattern {
        background-image: radial-gradient(#94a3b8 1px, transparent 1px);
        background-size: 24px 24px;
    }

    .dark .bg-dot-pattern {
        background-image: radial-gradient(#334155 1px, transparent 1px);
    }

    .glass-card {
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.5);
    }

    .dark .glass-card {
        background: rgba(30, 41, 59, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.05);
    }
</style>

@* ════════════════════════════════════════════════════════════════════════════
   MAIN CONTENT WRAPPER
   ════════════════════════════════════════════════════════════════════════════ *@
<div class="relative min-h-screen pb-12">

    @* ─────────────────────────────────────────────────────────────────
       HEADER SECTION
       ───────────────────────────────────────────────────────────────── *@
    <div class="relative z-10 -mx-6 lg:-mx-8 px-6 lg:px-8 py-8 mb-8 bg-gradient-to-r from-white via-gray-50/50 to-white dark:from-petrol-900 dark:via-petrol-900/50 dark:to-petrol-900 border-b border-gray-200 dark:border-petrol-800 shadow-sm">
        <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 animate__animated animate__fadeInDown">
            <div>
                <h1 class="text-3xl font-black text-petrol-900 dark:text-white tracking-tight flex items-center gap-3">
                    <span class="w-1.5 h-8 rounded-full bg-accent-500 shadow-lg shadow-accent-500/30"></span>
                    Çağrı Listesi
                </h1>
                <p class="text-sm font-medium text-gray-500 dark:text-gray-400 mt-2 ml-5">
                    @if (Model.TotalCount > 0)
                    {
                        <text>@Model.GetStartRecord() - @Model.GetEndRecord() arası gösteriliyor (Toplam <strong>@Model.TotalCount</strong> kayıt)</text>
                    }
                    else
                    {
                        <text>Kayıt bulunamadı</text>
                    }
                </p>
            </div>

            <a asp-page="./Olustur" class="group relative px-6 py-3 bg-gradient-to-r from-petrol-600 to-petrol-800 hover:from-petrol-700 hover:to-petrol-900 text-white font-bold rounded-xl shadow-lg shadow-petrol-500/20 hover:shadow-petrol-500/30 hover:-translate-y-0.5 transition-all duration-300" aria-label="Yeni Çağrı Ekle">
                <span class="flex items-center gap-2">
                    <i class="fas fa-plus group-hover:rotate-90 transition-transform"></i>
                    Yeni Çağrı Ekle
                </span>
            </a>
        </div>
    </div>

    @* ─────────────────────────────────────────────────────────────────
       CONTENT WRAPPER
       ───────────────────────────────────────────────────────────────── *@
    <div class="relative z-10 space-y-6 max-w-7xl mx-auto px-0">

        @* ─────────────────────────────────────────────────────────────────
           FILTER AREA (Gelişmiş Filtreleme)
           ───────────────────────────────────────────────────────────────── *@
        <div class="glass-card rounded-2xl p-6 shadow-sm animate__animated animate__fadeInUp">
            <form method="get" class="grid grid-cols-1 md:grid-cols-12 gap-4 items-end" id="filterForm">

                @* Arama (Floating Label) *@
                <div class="md:col-span-4 relative group">
                    <input type="text"
                           name="Filtre.Arama"
                           value="@Model.Filtre.Arama"
                           id="filter_search"
                           class="block px-2.5 pb-2.5 pt-5 w-full text-sm text-gray-900 dark:text-white bg-gray-50/50 dark:bg-petrol-900/30 rounded-xl border border-gray-200 dark:border-petrol-600 appearance-none focus:outline-none focus:ring-0 focus:border-accent-500 peer pl-10"
                           placeholder=" "
                           aria-label="Başlık veya Açıklama Ara" />
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <i class="fas fa-search text-gray-400 transition-colors peer-focus:text-accent-500"></i>
                    </div>
                    <label for="filter_search" class="absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-4 scale-75 top-4 z-10 origin-[0] left-10 peer-focus:text-accent-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-placeholder-shown:top-3.5 peer-focus:scale-75 peer-focus:-translate-y-4 pointer-events-none">
                        Başlık veya Açıklama Ara
                    </label>
                </div>

                @* Tür Filtresi (Floating Label) - DÜZELTİLDİ *@
                <div class="md:col-span-3 relative group">
                    <select name="Filtre.Turu"
                            id="filter_type"
                            asp-for="Filtre.Turu"
                            asp-items="Html.GetEnumSelectList<YenilikciEgitimPlatformu.Models.CagriTuru>()"
                            class="block px-2.5 pb-2.5 pt-5 w-full text-sm text-gray-900 dark:text-white bg-gray-50/50 dark:bg-petrol-900/30 rounded-xl border border-gray-200 dark:border-petrol-600 appearance-none focus:outline-none focus:ring-0 focus:border-accent-500 peer pl-10"
                            aria-label="Çağrı Türü Filtresi">
                        <option value="">Tüm Türler</option>
                    </select>
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <i class="fas fa-tag text-gray-400 transition-colors"></i>
                    </div>
                    <div class="absolute inset-y-0 right-0 flex items-center pr-3 pointer-events-none">
                        <i class="fas fa-chevron-down text-gray-400 text-xs"></i>
                    </div>
                    <label for="filter_type" class="absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-4 scale-75 top-4 z-10 origin-[0] left-10 peer-focus:text-accent-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-placeholder-shown:top-3.5 peer-focus:scale-75 peer-focus:-translate-y-4 pointer-events-none">
                        Çağrı Türü
                    </label>
                </div>

                @* Kurum Filtresi (Floating Label) *@
                <div class="md:col-span-3 relative group">
                    <input type="text"
                           name="Filtre.Kurum"
                           value="@Model.Filtre.Kurum"
                           id="filter_org"
                           class="block px-2.5 pb-2.5 pt-5 w-full text-sm text-gray-900 dark:text-white bg-gray-50/50 dark:bg-petrol-900/30 rounded-xl border border-gray-200 dark:border-petrol-600 appearance-none focus:outline-none focus:ring-0 focus:border-accent-500 peer pl-10"
                           placeholder=" "
                           aria-label="Kurum Filtresi" />
                    <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                        <i class="fas fa-building text-gray-400 transition-colors peer-focus:text-accent-500"></i>
                    </div>
                    <label for="filter_org" class="absolute text-sm text-gray-500 dark:text-gray-400 duration-300 transform -translate-y-4 scale-75 top-4 z-10 origin-[0] left-10 peer-focus:text-accent-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-placeholder-shown:top-3.5 peer-focus:scale-75 peer-focus:-translate-y-4 pointer-events-none">
                        Kurum
                    </label>
                </div>

                @* Action Buttons *@
                <div class="md:col-span-2 flex gap-2">
                    <button type="submit"
                            class="flex-1 h-[46px] bg-accent-500 hover:bg-accent-600 text-white rounded-xl transition-colors shadow-md shadow-accent-500/20 flex items-center justify-center gap-2"
                            aria-label="Filtrele">
                        <i class="fas fa-filter"></i>
                        <span class="hidden md:inline">Ara</span>
                    </button>
                    <a asp-page="./Index"
                       class="h-[46px] w-[46px] flex items-center justify-center bg-gray-100 dark:bg-petrol-700 text-gray-600 dark:text-gray-300 rounded-xl hover:bg-gray-200 dark:hover:bg-petrol-600 transition-colors"
                       title="Filtreleri Temizle"
                       aria-label="Filtreleri Temizle">
                        <i class="fas fa-redo"></i>
                    </a>
                </div>

            </form>
        </div>

        @* ─────────────────────────────────────────────────────────────────
           DATA GRID / CARDS
           ───────────────────────────────────────────────────────────────── *@
        @if (Model.TotalCount > 0)
        {
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 animate__animated animate__fadeInUp">
                @foreach (var cagri in Model.Data)
                {
                    <div class="glass-card rounded-2xl overflow-hidden shadow-sm hover:shadow-xl hover:-translate-y-1 transition-all duration-300 group">
                        @* Card Header *@
                        <div class="relative h-32 bg-gradient-to-br from-petrol-500 to-petrol-700 overflow-hidden">
                            <div class="absolute inset-0 opacity-20 bg-dot-pattern"></div>
                            <div class="absolute top-3 right-3">
                                <span class="px-3 py-1 rounded-full text-xs font-bold text-white bg-white/20 backdrop-blur-sm border border-white/30">
                                    @cagri.Turu.ToString()
                                </span>
                            </div>
                            <div class="absolute bottom-3 left-4 right-4">
                                <h3 class="text-lg font-bold text-white line-clamp-2 group-hover:line-clamp-none transition-all">
                                    @cagri.Baslik
                                </h3>
                            </div>
                        </div>

                        @* Card Body *@
                        <div class="p-5 space-y-3">
                            <div class="flex items-center text-sm text-gray-600 dark:text-gray-400">
                                <i class="fas fa-building w-4 mr-2"></i>
                                <span class="truncate">@cagri.Kurum</span>
                            </div>

                            <div class="flex items-center gap-4 text-xs text-gray-500 dark:text-gray-400">
                                <div class="flex items-center">
                                    <i class="fas fa-calendar-check w-4 mr-1.5"></i>
                                    @cagri.BaslangicTarihi?.ToString("dd.MM.yyyy")
                                </div>
                                @if (cagri.BitisTarihi.HasValue)
                                {
                                    <div class="flex items-center">
                                        <i class="fas fa-calendar-times w-4 mr-1.5"></i>
                                        @cagri.BitisTarihi?.ToString("dd.MM.yyyy")
                                    </div>
                                }
                            </div>

                            @if (!string.IsNullOrEmpty(cagri.KisaAciklama))
                            {
                                <p class="text-sm text-gray-600 dark:text-gray-300 line-clamp-2">
                                    @cagri.KisaAciklama
                                </p>
                            }
                        </div>

                        @* Card Footer *@
                        <div class="px-5 pb-5 flex gap-2">
                            <a asp-page="./Detay" asp-route-id="@cagri.Id"
                               class="flex-1 py-2.5 text-center text-sm font-semibold text-petrol-700 dark:text-petrol-300 bg-petrol-50 dark:bg-petrol-900/30 rounded-xl hover:bg-petrol-100 dark:hover:bg-petrol-800 transition-colors"
                               aria-label="Görüntüle">
                                <i class="fas fa-eye mr-1"></i> Görüntüle
                            </a>
                            <a asp-page="./Duzenle" asp-route-id="@cagri.Id"
                               class="flex-1 py-2.5 text-center text-sm font-semibold text-amber-700 dark:text-amber-400 bg-amber-50 dark:bg-amber-900/20 rounded-xl hover:bg-amber-100 dark:hover:bg-amber-900/30 transition-colors"
                               aria-label="Düzenle">
                                <i class="fas fa-edit mr-1"></i> Düzenle
                            </a>
                            <button type="button"
                                    onclick="handleDelete(@cagri.Id, '@Html.Raw(cagri.Baslik.Replace("'", "\\'"))')"
                                    class="flex-1 py-2.5 text-center text-sm font-semibold text-red-700 dark:text-red-400 bg-red-50 dark:bg-red-900/20 rounded-xl hover:bg-red-100 dark:hover:bg-red-900/30 transition-colors"
                                    aria-label="Sil">
                                <i class="fas fa-trash mr-1"></i> Sil
                            </button>
                        </div>
                    </div>
                }
            </div>

            @* ─────────────────────────────────────────────────────────────────
               PAGINATION (Query String Preserved)
               ───────────────────────────────────────────────────────────────── *@
            @if (Model.GetTotalPages() > 1)
            {
                <div class="glass-card rounded-2xl p-6 mt-8 animate__animated animate__fadeInUp">
                    <nav class="flex flex-col md:flex-row items-center justify-between gap-4" aria-label="Pagination">
                        <p class="text-sm text-gray-600 dark:text-gray-400">
                            Sayfa <strong>@Model.Filtre.Page</strong> / <strong>@Model.GetTotalPages()</strong>
                        </p>

                        <div class="flex gap-2">
                            @if (Model.Filtre.Page > 1)
                            {
                                <a asp-page="./Index"
                                   asp-route-Filtre.Page="@(Model.Filtre.Page - 1)"
                                   asp-route-Filtre.Arama="@Model.Filtre.Arama"
                                   asp-route-Filtre.Turu="@Model.Filtre.Turu"
                                   asp-route-Filtre.Kurum="@Model.Filtre.Kurum"
                                   asp-route-Filtre.PageSize="@Model.Filtre.PageSize"
                                   asp-route-Filtre.SortBy="@Model.Filtre.SortBy"
                                   class="px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-petrol-800 rounded-lg transition-colors"
                                   aria-label="Önceki Sayfa">
                                    <i class="fas fa-chevron-left mr-1"></i> Önceki
                                </a>
                            }

                            @* Sayfa Numaraları *@
                            @for (int i = Math.Max(1, Model.Filtre.Page - 2); i <= Math.Min(Model.GetTotalPages(), Model.Filtre.Page + 2); i++)
                            {
                                <a asp-page="./Index"
                                   asp-route-Filtre.Page="@i"
                                   asp-route-Filtre.Arama="@Model.Filtre.Arama"
                                   asp-route-Filtre.Turu="@Model.Filtre.Turu"
                                   asp-route-Filtre.Kurum="@Model.Filtre.Kurum"
                                   asp-route-Filtre.PageSize="@Model.Filtre.PageSize"
                                   asp-route-Filtre.SortBy="@Model.Filtre.SortBy"
                                   class="w-10 h-10 flex items-center justify-center text-sm font-medium rounded-lg transition-colors @(i == Model.Filtre.Page ? "bg-accent-500 text-white" : "text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-petrol-800")"
                                   aria-label="Sayfa @i">
                                    @i
                                </a>
                            }

                            @if (Model.Filtre.Page < Model.GetTotalPages())
                            {
                                <a asp-page="./Index"
                                   asp-route-Filtre.Page="@(Model.Filtre.Page + 1)"
                                   asp-route-Filtre.Arama="@Model.Filtre.Arama"
                                   asp-route-Filtre.Turu="@Model.Filtre.Turu"
                                   asp-route-Filtre.Kurum="@Model.Filtre.Kurum"
                                   asp-route-Filtre.PageSize="@Model.Filtre.PageSize"
                                   asp-route-Filtre.SortBy="@Model.Filtre.SortBy"
                                   class="px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-petrol-800 rounded-lg transition-colors"
                                   aria-label="Sonraki Sayfa">
                                    Sonraki <i class="fas fa-chevron-right ml-1"></i>
                                </a>
                            }
                        </div>
                    </nav>
                </div>
            }
        }
        else
        {
            @* ─────────────────────────────────────────────────────────────────
               EMPTY STATE
               ───────────────────────────────────────────────────────────────── *@
            <div class="flex flex-col items-center justify-center py-20 px-4 text-center glass-card rounded-3xl border border-dashed border-gray-300 dark:border-petrol-700 animate__animated animate__fadeIn">
                <div class="w-24 h-24 bg-gray-50 dark:bg-petrol-800 rounded-full flex items-center justify-center mb-6 shadow-inner ring-8 ring-gray-50/50 dark:ring-petrol-800/50">
                    <i class="fas fa-search text-4xl text-gray-300 dark:text-petrol-600"></i>
                </div>
                <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">Sonuç Bulunamadı</h3>
                <p class="text-gray-500 dark:text-gray-400 max-w-md mx-auto mb-8">
                    Aradığınız kriterlere uygun çağrı bulunamadı. Filtreleri temizlemeyi deneyin veya yeni bir çağrı oluşturun.
                </p>
                <div class="flex gap-3">
                    <a asp-page="./Index" class="px-6 py-3 bg-gray-100 dark:bg-petrol-800 text-gray-700 dark:text-gray-300 font-semibold rounded-xl hover:bg-gray-200 dark:hover:bg-petrol-700 transition-all">
                        <i class="fas fa-redo mr-2"></i>Filtreleri Temizle
                    </a>
                    <a asp-page="./Olustur" class="group relative px-6 py-3 bg-gradient-to-r from-petrol-600 to-petrol-800 hover:from-petrol-700 hover:to-petrol-900 text-white font-bold rounded-xl shadow-lg shadow-petrol-500/20 hover:shadow-petrol-500/30 hover:-translate-y-0.5 transition-all duration-300">
                        <span class="flex items-center gap-2">
                            <i class="fas fa-plus group-hover:rotate-90 transition-transform"></i>
                            Yeni Çağrı Ekle
                        </span>
                    </a>
                </div>
            </div>
        }

    </div>
</div>

@* ════════════════════════════════════════════════════════════════════════════
   HIDDEN DELETE FORM (CSRF Protected)
   ════════════════════════════════════════════════════════════════════════════ *@
<form id="deleteForm" method="post" asp-page-handler="Delete" class="hidden">
    <input type="hidden" name="id" id="deleteId" />
    @Html.AntiForgeryToken()
</form>

@* ════════════════════════════════════════════════════════════════════════════
   JAVASCRIPT
   ════════════════════════════════════════════════════════════════════════════ *@
@section Scripts {
    <script type="module">
        // ═══════════════════════════════════════════════════════════════════
        // ALERTSERVICE ENTEGRASYONU (Merkezi Feedback Sistemi)
        // ═══════════════════════════════════════════════════════════════════

        import AlertService from '/js/modules/alertService.js';

        /**
         * Silme işlemi için merkezi fonksiyon
         * AlertService.confirmDelete kullanır (inline Swal YOK!)
         * @@param {number} id - Silinecek çağrı ID
         * @@param {string} baslik - Çağrı başlığı (Onay mesajında gösterilir)
         */
        window.handleDelete = async function(id, baslik) {
            // [Mimari Notu]
            // Doğrudan Swal.fire() kullanımı YASAK!
            // Tüm kullanıcı geri bildirimleri AlertService üzerinden yapılır.

            const confirmed = await AlertService.confirmDelete(
                `"${baslik}" başlıklı çağrı ve ilişkili tüm veriler kalıcı olarak silinecek.\n\nBu işlem geri alınamaz!`,
                'Silmek İstediğinize Emin misiniz?'
            );

            if (confirmed) {
                // [Kullanıcı Deneyimi]
                // Yükleme göstergesi ile kullanıcıya işlem devam ediyor mesajı verilir
                AlertService.showLoading('Çağrı siliniyor...');

                // [Form Submit]
                // CSRF token ile korumalı form gönderimi
                document.getElementById('deleteId').value = id;
                document.getElementById('deleteForm').submit();
            }
        }

        // ═══════════════════════════════════════════════════════════════════
        // AUTO-SUBMIT FİLTRELERİ (Debounce ile Optimize Edilmiş)
        // ═══════════════════════════════════════════════════════════════════

        const filterForm = document.getElementById('filterForm');
        let debounceTimer = null;

        // [Performans]
        // Text input'lar için debounce (600ms)
        // Kullanıcı yazmayı bıraktığında otomatik arama yapılır
        const textInputs = filterForm.querySelectorAll('#filter_search, #filter_org');
        textInputs.forEach(input => {
            input.addEventListener('input', function() {
                clearTimeout(debounceTimer);
                debounceTimer = setTimeout(() => {
                    filterForm.submit();
                }, 600);
            });
        });

        // [Kullanıcı Deneyimi]
        // Dropdown için hemen submit (Debounce gerekmez)
        const typeFilter = document.getElementById('filter_type');
        if (typeFilter) {
            typeFilter.addEventListener('change', function() {
                filterForm.submit();
            });
        }
    </script>
}

@* ════════════════════════════════════════════════════════════════════════════
   SAYFA AÇIKLAMASI
   ════════════════════════════════════════════════════════════════════════════
   
   [AMAÇ]
   Admin kullanıcıları için Çağrı Bilgileri yönetim sayfası.
   Modern, responsive, glassmorphism tasarımlı liste görünümü.
   
   [ÖZELLİKLER]
   - Responsive Card Grid Layout (3 sütun desktop, 2 sütun tablet, 1 sütun mobil)
   - Gelişmiş Filtreleme (Arama, Tür, Kurum)
   - Pagination (Query String Korumalı - Filtreler URL'de saklanır)
   - Lazy Loading Images (loading="lazy")
   - AlertService Entegrasyonu (Merkezi Feedback)
   - Accessibility Compliant (ARIA labels, keyboard navigation)
   - Dark Mode Support (localStorage ile kalıcı)
   
   [MİMARİ]
   - ❌ ASLA inline Swal.fire() kullanma
   - ✅ HER ZAMAN AlertService modülü kullan
   - ES Module import yapısı (import/export)
   - Region kullanımı ile kod organizasyonu
   - TempData ile server-side mesajlar (_LayoutDashboard'da otomatik gösterilir)
   
   [PERFORMANS]
   - Lazy loading images (loading="lazy")
   - Debounced auto-submit (600ms) - Gereksiz server çağrılarını önler
   - CSS transitions (GPU accelerated)
   - Pagination ile sayfa başına 12 kayıt
   
   [GÜVENLİK]
   - CSRF token koruması (@Html.AntiForgeryToken())
   - XSS koruması (Razor automatic encoding)
   - SQL Injection koruması (EF Core parameterized queries)
   - Input validation (Model binding)
   
   [ÇAĞRI TÜRÜ DROPDOWN ÇÖZÜMÜ]
   - asp-for="Filtre.Turu" ile model binding sağlandı
   - Seçili değer artık doğru görünüyor
   - URL'de enum ismi yerine değeri görünmesi normal (asp-route ile kontrol edilir)
   - Dark mode'da dropdown rengi uyumlu
   
   [BAĞIMLILIKLAR]
   - /js/modules/alertService.js (Merkezi feedback sistemi)
   - _LayoutDashboard.cshtml (Layout)
   - Tailwind CSS (Utility-first CSS framework)
   - Font Awesome 6.4 (İkon kütüphanesi)
   - SweetAlert2 (AlertService tarafından kullanılır)
*@
