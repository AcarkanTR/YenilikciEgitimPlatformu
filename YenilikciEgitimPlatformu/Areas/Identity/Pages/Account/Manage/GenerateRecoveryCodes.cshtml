@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Kurtarma Kodları Oluştur";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<div class="max-w-2xl">
    <partial name="_StatusMessage" for="StatusMessage" />

    <div class="mb-6">
        <h3 class="text-2xl font-bold text-gray-900 dark:text-white flex items-center gap-2">
            <i class="fas fa-file-code text-accent-500"></i> @ViewData["Title"]
        </h3>
    </div>

    <div class="bg-yellow-50 dark:bg-yellow-900/10 border border-yellow-200 dark:border-yellow-800 rounded-xl p-6 mb-8 animate__animated animate__pulse">
        <div class="flex items-start">
            <div class="flex-shrink-0">
                <i class="fas fa-exclamation-triangle text-yellow-600 text-xl mt-1"></i>
            </div>
            <div class="ml-3">
                <h3 class="text-sm font-bold text-yellow-800 dark:text-yellow-200">Bu kodları güvenli bir yerde saklayın.</h3>
                <div class="mt-2 text-sm text-yellow-700 dark:text-yellow-300 space-y-2">
                    <p>Eğer cihazınızı kaybederseniz ve kurtarma kodlarınız yoksa, hesabınıza erişimi kalıcı olarak kaybedebilirsiniz.</p>
                    <p class="text-xs italic">Not: Yeni kodlar oluşturmak, mevcut kodları geçersiz kılar ve authenticator anahtarlarını değiştirmez.</p>
                </div>
            </div>
        </div>
    </div>

    <form method="post">
        <button class="group w-full sm:w-auto flex justify-center items-center gap-2 py-3 px-6 border border-transparent text-sm font-bold rounded-xl text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 shadow-lg hover:shadow-red-500/30 transition-all hover:-translate-y-0.5" type="submit">
            <i class="fas fa-sync-alt group-hover:rotate-180 transition-transform duration-500"></i>
            Yeni Kodlar Oluştur
        </button>
    </form>
</div>